// Generated by LiveScript 1.4.0
(function(){
  var createYTPlayer, $T, postj, getj, getPage, out$ = typeof exports != 'undefined' && exports || this;
  out$.createYTPlayer = createYTPlayer = function(a){
    var width, height, autoplay, url, frame;
    if ($(a.container)) {
      width = a.w || 640;
      height = a.h || 390;
      autoplay = a.autoplay || 0;
      url = "http://www.youtube.com/embed/" + a.id + "?autoplay=" + autoplay;
      frame = $('<iframe></iframe>').attr('id', 'ytplayer').attr('type', 'text/html').attr('width', width).attr('height', height).attr('src', url);
      return $(a.container).append(frame);
    }
  };
  out$.$T = $T = function(tag){
    return "<" + tag + "><" + tag + "/>";
  };
  out$.postj = postj = function(data){
    return $.post('ajax', data, 'json');
  };
  out$.getj = getj = function(data){
    return $.getJSON('ajax', data);
  };
  out$.getPage = getPage = function(page, myDone){
    return $.get('ajax', {
      name: page
    }).done(function(data){
      $('#container').empty().append(data);
      return typeof myDone == 'function' ? myDone(data) : void 8;
    }).fail(function(data){
      var errorbox;
      return errorbox = $('#container').append($T(div));
    });
  };
}).call(this);
